name: pixlite_scanner_audit
description: Temporary forensic diagnostics build for PixLite's Google ML Kit document-scanner flow. Not a product build.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.10.0-0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  google_mlkit_document_scanner: ^0.5.0
  path_provider: ^2.1.5
  share_plus: ^11.0.0
  crypto: ^3.0.3
  image: ^4.8.0
  archive: ^3.6.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
